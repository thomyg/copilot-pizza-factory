# --- .NET build output ---
[Bb]in/
[Oo]bj/
[Oo]ut/
artifacts/
*.user
*.suo
.vs/
.vscode/
*.userprefs

# --- Rider / JetBrains ---
.idea/

# --- azd (Azure Developer CLI) local env state — per-tenant values, never publish ---
.azure/

# --- Local-only journey log (not for the public repo) ---
PROGRESS.md

# --- Built Teams app package (concrete tenant-specific IDs; the manifest in source is templated) ---
src/PizzaFactory.GiuseppeBot/appManifest/*.zip

# --- macOS / editor cruft ---
.DS_Store
Thumbs.db

# --- Secrets / local config (never publish) ---
*.secrets.json
appsettings.*.Local.json
*.env
.env

# --- Internal tooling & docs kept out of the public repo ---
# Agent / Claude scaffolding
CLAUDE.md
AGENTS.md
.claude/
# Beads issue tracker (contains internal notes/IDs)
.beads/
# Ops scripts and internal setup runbook (org-specific)
scripts/
docs/
